// 

POST /verify

{
  jobId, 
  taskId,
  responseId,
  workerId,
  verifierId,
  score,
  reason,
}

 score {
  id,
  created_at,
  jobId, 
  taskId,
  responseId,
  workerId,
  verifierId,
  score,
  reason,
}